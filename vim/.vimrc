syntax enable

colorscheme solarized

set number
set background=dark
set guifont=JetBrains\ Mono:h14
set colorcolumn=80
set whichwrap+=<,>,[,]

if has('mac') && has('gui_running')
  set macligatures
endif
