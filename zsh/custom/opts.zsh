# Opts
# ====

# `cd` into a directory by typing its name alone.
setopt AUTO_CD

# Push every visited directory onto the dir stack.
setopt AUTO_PUSHD

# Keep the dir stack free of duplicate entries.
setopt PUSHD_IGNORE_DUPS

# Swap +/- in `cd -n` dir stack references.
setopt PUSHDMINUS
