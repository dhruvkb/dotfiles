# AWS CLI
# =======

# Configure AWS to store the config and credential files in the dotfiles
# repository.
export AWS_CONFIG_FILE="$HOME/dotfiles/aws/config"
export AWS_SHARED_CREDENTIALS_FILE="$HOME/dotfiles/aws/credentials"
