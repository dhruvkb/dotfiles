#!/bin/sh
branch=$(git rev-parse --abbrev-ref HEAD)
echo "back: resets $branch to origin/$branch, discarding uncommitted changes and unpushed commits."
printf 'Press Enter to continue, Ctrl+C to abort: '
read _ </dev/tty || exit 1
git reset --hard "origin/$branch"
