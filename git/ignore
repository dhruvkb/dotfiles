# Keep this list as small as possible, because if you want something excluded
# from Git, chances are other collaborators want to do the same.

# `pls`
# -----
# `pls` is so niche that projects' `.gitignore`s don't exclude its config.
.pls.yml
